# Stabilize the authorized native app — Terra medium only

Project: /Users/joaopaulo/Documents/Projetos/Pessoal/OrquestradorLocal. The user authorizes finishing implementation through codex2, capped at gpt-5.6-terra with medium reasoning. Do not escalate models or delegate. Preserve current files and all prior reports. Read current project context, contexto/ASSIGNMENT-CODEX2-TERRA-CORRECOES.md and both reports in execucoes/2026-09-06-codex2-terra-correcao/. All original scope/safety constraints remain: write only this project and uniquely owned synthetic fixture/temp outputs; no real pilots/startup migrations, unrelated projects, Vault edits, private data, Git, global installations, force kill or human acceptance claims. Do not alter runners/assignments/execution controls. Update context before/after real changes.

Finish these independently authorized corrections without asking for repeated approval:
1. Fix the reproducible CatalogStoreTests asynchronous Task/defer vs tearDown race (around lines 107 and 14). Cleanup must be awaited deterministically. Verify all test-generated files are removed only if owned, and the full suite passes consistently, not just filtered tests. Recheck first-save recovery and correct the matrix after the previously failed suite.
2. Fix FixtureProductIntegrationTests ownership timing (around line 58): bootstrap may succeed before startService fails, leaving ownsJob false and leaking the job. Track ownership robustly from creation/registration, handle partial failures, and ensure cleanup executes on every failure path while preserving foreign jobs/files. Add real regression tests for preexisting foreign artifact preservation and bootstrap-success/start-failure cleanup.
3. Propagate caller cancellation through LaunchAgentAdapter's detached subprocess work without blocking the UI or terminating a managed user service. Cancel only observation/helper processes created by this invocation. Eliminate Swift concurrency warnings with correct synchronization, not unsafe annotations that hide races. Test cancellation, pipe saturation, truncation and timeout.
4. Complete meaningful real Swift product integration coverage for T04, T09–T12: concurrent actions/instances, graceful-stop timeout that does not falsely report stopped or force-kill, foreign port/process preservation, PID/executable mismatch, reopening/reconciliation. Use unique isolated helper fixtures and observable assertions. Do not substitute direct launchctl-only testing for product behavior or mark an implemented but unexecuted scenario passed.
5. Remove unsupported visual acceptance claims. Black compact-dark capture is invalid. Attempt actual app launch and usable captures of your own app with synthetic populated profiles in both themes and small window. Check text colors used in RegistrationView/ServiceCard/MenuBarPopover, not only ten passing semantic pairs. Check keyboard/focus and available accessibility inspection. Use task-scoped app data, do not capture private windows. If unavailable, document the exact tool/permission limitation and retain unverified status.
6. Measure the actual packaged app in idle (not the test harness): 10-minute RSS point and 5-minute CPU sample if technically feasible, alongside observation subprocesses and baseline. Starting this new app with only task-owned synthetic catalog is authorized. Opening the built package by macOS launch mechanisms is authorized; no need to ask João to run a terminal for that test. Do not disable system protections. Close only the app instance you started after checks, preserving any preexisting user instance.

Use at most three distinct meaningful attempts per blocker, then document the concrete limitation and finish other independent work. Do not stop early merely because G1/T23 need human participation. Keep real-pilot G1 and human T23 open. Save concise evidence, update acceptance matrix, package corrected .app and run full regression, release build, signature/plist and fixture cleanup verification. Distinguish technical validation from human acceptance. No screenshots-as-proof without visually inspecting them. The external runner runs an independent review afterward.

Final output is saved to execucoes/2026-09-06-codex2-terra-estabilizacao/RELATORIO-IMPLEMENTACAO.md. Return exactly ten numbered sections in Portuguese: 1. O que foi feito; 2. Arquivos alterados; 3. Arquivos analisados; 4. O que não foi alterado; 5. Testes executados; 6. Resultado dos testes; 7. Pendências; 8. Riscos; 9. Status final: OK ou FALHA; 10. Próximo passo recomendado. Include issue-by-issue evidence and EXACT blocker/reason for every independently actionable requirement left incomplete. No code/log dumps, secrets or personal data. Never claim V1 functional without mandatory gates.
