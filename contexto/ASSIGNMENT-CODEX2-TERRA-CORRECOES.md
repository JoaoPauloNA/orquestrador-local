# Terra medium — complete the independently actionable V1 work

The user authorized finishing the project using codex2 and capped execution at GPT-5.6 Terra / medium. You are the correction implementer, within that same authorization. Do not escalate models, spawn agents, alter runners, or ask for repeated approval for these authorized fixes. Preserve all existing work. Work only in /Users/joaopaulo/Documents/Projetos/Pessoal/OrquestradorLocal and allowed uniquely owned temporary fixture artifacts.

Read current INDEX.md, ESTADO_ATUAL.md, ROADMAP.md, CLAUDE_ORQUESTRADOR.md, contexto/ASSIGNMENT-CODEX2-TERRA-RETOMADA.md (its safety/scope requirements remain applicable), and BOTH reports in execucoes/2026-09-06-codex2-terra/. Read the actual code and original acceptance matrix before fixes. This is an implementation follow-up, not a planning-only task. Update context before/after when the state changes.

The independent review rejected technical V1. Resolve all demonstrated issues and all independent mandatory checks possible on this Mac, rather than marking tests skipped merely because human pilot gates exist:

1. Fix actual error-text contrast in ServiceDetailView and OrqDanger/OrqDangerBg for both themes. Audit actual rendered text/background pairs (not a handpicked passing list), expand the contrast check and reconcile incorrect design/DESIGN-SYSTEM.md ratios. Keep the intended CasaCapital-inspired native design.
2. LaunchAgentAdapter currently waits for subprocess exit before draining pipes and has unbounded output/no timeout. Implement concurrent bounded stdout/stderr consumption, bounded command duration, and honest cancellation/errors without blocking UI. Do not kill a managed user service to solve a hanging observation command. Add meaningful tests for pipe saturation, timeouts and output limits using uniquely owned helper processes.
3. CatalogStore must recover the most recent validated catalog even when corruption happens after the FIRST successful persistence. Use atomic durable safe writes/backup recovery and meaningful first-save/subsequent-save/corruption/interrupted-write tests. Do not erase user data on recovery failure.
4. Fixture integration and ProfileValidator tests currently use fixed names/ports with destructive cleanup. Make each run collision-safe with unique task-owned artifacts, explicit preexistence guards and cleanup based on tracked ownership. Do not overwrite/delete any preexisting plist or stop an existing job. Add regression coverage that preexisting foreign artifacts survive.
5. The fixture test called launchctl/curl directly, so it did not validate the Swift product. Add and RUN actual integration through LaunchAgentAdapter/OrchestrationCoordinator against a real isolated fixture: readiness, busy/unknown activity, serial/concurrent actions, collision, owned graceful stop, external preservation, PID/executable mismatch, timeout and reopen/reconciliation. Keep activity and service state separate; no force kill. Complete T02/T04–T12/T13/T14/T20/T21 fixture portions with evidence as feasible; do not use mocks as proof of real product integration.
6. Replace the entirely black compact-dark evidence with visually inspected real native UI evidence showing registered synthetic services and relevant states in light, dark and a small window. Check keyboard/focus/VoiceOver capabilities honestly; run available checks rather than assuming all UI requires João. Synthetic service testing through your own app is authorized. Never capture unrelated private windows/data. If real capture is unavailable, say exactly why rather than create placeholder proof.
7. Measure the app and observation subprocess resource budget (T16) with documented intervals and cleanly scoped synthetic workload. Package the corrected native .app, verify signature/plist/executable, and test launching that package via the available macOS mechanism (without disabling Gatekeeper/SIP); distinguish actual launch evidence from human Finder interaction.

Do not operate or migrate real mflux-studio/ComfyUI pilots, alter their LaunchAgents, modify Vault/CasaCapital/Athena, initialize Git, commit/push/release, install global tooling, expose credentials, force-kill services, or counterfeit G1/T23. Those human/real-pilot gates remain open. Their existence does not justify leaving independent code fixes/tests incomplete. Keep G0–G3 claims faithful. No repeated unbounded repair attempts: after three distinct failed approaches on an issue, document the blocker and finish other authorized items.

Run proportionate regression tests, actual fixture integration, release build, package and visual/contrast verification after changes. Save sanitized reproducible evidence and update the T01–T23 matrix distinguishing fully passed, partial and not executed. Correct any inaccurate previously reported claims. The runner launches a fresh independent reviewer after you end; do not launch it yourself.

Your final output is automatically saved as execucoes/2026-09-06-codex2-terra-correcao/RELATORIO-IMPLEMENTACAO.md. Return exactly ten numbered Portuguese sections, no code/log dumps, secrets or personal data: 1. O que foi feito; 2. Arquivos alterados; 3. Arquivos analisados; 4. O que não foi alterado; 5. Testes executados; 6. Resultado dos testes; 7. Pendências; 8. Riscos; 9. Status final: OK ou FALHA; 10. Próximo passo recomendado. Include a compact issue-by-issue resolution/evidence list, actual test counts, .app path, gate status, missing checks and human tasks. Do not label V1 functional if mandatory acceptance remains incomplete.
