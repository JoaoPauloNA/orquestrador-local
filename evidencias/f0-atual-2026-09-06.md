# F0 atual — observação somente leitura

Data: 2026-09-06. Classificação: PARCIAL. Nenhum piloto foi iniciado, parado, reiniciado, migrado ou editado.

## Observações atuais

- Supervisor do usuário observado: `gui/501` (`launchctl` acessível).
- mflux-studio: os dois plists históricos existem e continuam carregados: `local.mflux-studio` (última saída 126) e `com.joaopaulo.mflux-studio` (PID observado). Ambos declaram `KeepAlive` e `RunAtLoad`; a duplicidade bloqueia controle até G1. O primeiro declara `/bin/bash`; o segundo declara o Python da venv do projeto. Os caminhos de log foram somente identificados, sem leitura.
- ComfyUI: instalação encontrada em `~/ComfyUI`; o LaunchAgent `local.comfyui` está carregado, declara o Python da venv e `KeepAlive`/`RunAtLoad`. `http://127.0.0.1:8188/system_stats` respondeu 200 com chaves de topo `devices` e `system`; `/queue` respondeu 200 com 0 em execução e 0 pendentes no instante observado.
- Isso confirma que o endpoint `/queue` estava disponível nesta observação; não se faz a alegação categórica de que ComfyUI não possui endpoint de fila. Para mflux-studio, a inspeção limitada de `server.py` não encontrou rota HTTP de health/fila; a atividade permanece **DESCONHECIDA**, não ociosa.
- Raycast está instalado. Não foi verificado um recurso nativo equivalente de gestão segura de LaunchAgents; portanto essa capacidade é NÃO VERIFICADA.

## Limites e próxima ação

G0 segue aberto: falta revisar a estratégia do mflux-studio, a identidade de prontidão dele e aprovar os contratos. G1 segue obrigatório antes de qualquer mutação em pilotos. A observação F0 não é evidência de aceitação V1.
